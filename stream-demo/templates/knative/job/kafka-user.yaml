apiVersion: kafka.strimzi.io/v1beta2
kind: KafkaUser
metadata:
  name: {{ .Values.knative.job.kafka.username | quote }}
  labels:
    strimzi.io/cluster: {{ .Values.kafka.cluster.name | quote }}
spec:
  authentication:
    type: scram-sha-512
  authorization:
    type: simple
    acls:
      - resource:
          type: topic
          name: {{ .Values.knative.job.kafka.topic | quote }}
          patternType: literal
        operations: 
        - Read
        - Describe
        host: '*'
      - resource:
          type: topic
          name: {{ .Values.knative.job.kafka.topic | quote }}
          patternType: literal
        operations: 
        - Read
        - Describe
        host: '*'
      - resource:
          type: group
          name: {{ .Values.knative.job.kafka.topic | quote }}
          patternType: literal
        operations: 
        - Read
        - Describe
        host: '*'
      - resource:
          type: group
          name: {{ .Values.knative.job.kafka.topic | quote }}
          patternType: literal
        operations: 
        - Read
        - Describe
        host: '*'    